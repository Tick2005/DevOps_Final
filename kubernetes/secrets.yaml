# =============================================================================
# SECRETS.YAML - Sensitive data for ProductX
# =============================================================================
# Giá trị sẽ được thay thế bởi CI/CD từ GitHub Secrets
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: productx
type: Opaque
stringData:
  POSTGRES_PASSWORD: "PLACEHOLDER_DB_PASSWORD"
  SPRING_DATASOURCE_PASSWORD: "PLACEHOLDER_DB_PASSWORD"

# =============================================================================
# FRONTEND SERVICE - ProductX Frontend
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: productx
  labels:
    app: frontend
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

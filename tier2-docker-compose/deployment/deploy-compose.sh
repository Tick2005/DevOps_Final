#!/usr/bin/env bash
set -euo pipefail

# TODO: replace APP_VERSION with CI-provided tag
# Example: APP_VERSION=$1 docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

echo "Tier 2 deploy template. Fill me."

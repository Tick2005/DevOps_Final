pipeline {
  agent any

  environment {
    BUILD_CONTAINER = 'true'
    IMAGE_TAG = "${env.GIT_COMMIT}"
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Install') {
      steps {
        dir('tier5-kubernetes/backend') {
          sh 'npm ci'
        }
      }
    }

    stage('Lint') {
      steps {
        dir('tier5-kubernetes/backend') {
          sh 'npm run lint --if-present'
        }
      }
    }

    stage('Build') {
      steps {
        dir('tier5-kubernetes/backend') {
          sh 'npm run build --if-present'
          sh 'tar -czf ../../app-artifact.tgz .'
        }
      }
    }

    stage('Security Scan') {
      steps {
        sh 'trivy fs --exit-code 1 --severity HIGH,CRITICAL tier5-kubernetes/backend'
      }
    }

    stage('Build and Push Image') {
      when {
        expression { env.BUILD_CONTAINER == 'true' }
      }
      steps {
        sh 'echo Set registry login here'
        sh 'docker build -t your-registry/final-app:${IMAGE_TAG} tier5-kubernetes/backend'
        sh 'docker push your-registry/final-app:${IMAGE_TAG}'
      }
    }

    stage('Deploy') {
      steps {
        sh 'chmod +x tier5-kubernetes/deployment/deploy-k8s.sh'
        sh 'tier5-kubernetes/deployment/deploy-k8s.sh'
      }
    }
  }
}
